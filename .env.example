# Public browser configuration only. Never put signing keys in VITE_* variables.
VITE_MONAD_RPC_URL=https://testnet-rpc.monad.xyz
# Set after T06 deployment. The page stays readable without it, but chain reads/writes remain disabled.
VITE_CONTRACT_ADDRESS=
